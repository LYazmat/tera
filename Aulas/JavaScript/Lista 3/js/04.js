/*
4 - Crie uma função que retorne a soma entre dois números.
    Exemplo 1:
        Entrada:
            soma(3, 4)
        Saída:
            3 + 4 = 7
*/

let soma = (a, b) => {
    return a + b;
}

let a = 3, b = 4;
console.log(`${a} + ${b} = ${soma(a, b)}`);